════════════════════════════════════════════════════════════════════
 PHASE 4A — S9: Regression & Docs Gate
 My Life OS — Brain Intelligence Engine (BIE)
════════════════════════════════════════════════════════════════════

 สิ่งแรกที่ต้องทำ:
 1. อ่าน /doc/STATE.md (current step = S9, constraints, allowed files)
 2. ปฏิบัติตาม /doc/STANDING_INSTRUCTIONS.md (ทุก workflow rule)

 PRIMARY DELIVERABLE (S9 = Final Regression & Doc Gate):
 - Regression: npm run build Exit 0 + npm run lint Exit 0
 - Static trace: bieEnabled=false → S7 guard at RoomBrainRepository.ts L181 fires,
   ZERO semanticService/VectorIndex instantiation, output byte-identical Pre-4
 - Static trace: bieEnabled=true → hybridRankItems() runs, sorted hybridScore DESC returned
 - 7 aiService features trace (text/refine/analyze/suggest/insight/tag/learn)
   × bieEnabled T/F → no UX breakage
 - Phase 4A closeout: CHANGELOG S9 section, ROADMAP 4A → ✅ Complete 100%,
   STATE.md updated, prompt.text overwritten with 4B kickoff handoff

 WORK ORDER — EXECUTE IN ORDER:

 S9 Step 1/4 — Build + Lint Validation:
   npm run build = Exit 0 (must pass before proceeding)
   npm run lint  = Exit 0 (must pass before proceeding)

 S9 Step 2/4 — Static Trace (no code changes, read-only):
   a) RoomBrainRepository.ts L180-185: confirm bieEnabled===false guard returns legacy FIRST
   b) RoomBrainRepository.ts L296-375: confirm hybridSortedLegacy return path exists
   c) hybridScorer.ts: confirm HYBRID_WEIGHT_SUM IIFE throws if sum ≠ 1.0
   d) synonyms.ts: confirm validateSynonymDictionary() exported + dictionary has 50+ entries

 S9 Step 3/4 — Doc Closeout (TARGETED EDITS per SI-4, ≤15 lines per SI-2):
   CHANGELOG.md: append S9 section at TOP
   ROADMAP.md: S9 row → ✅ Complete; 4A progress → ✅ Complete 100%; banner → Done
   STATE.md: update Current Step → 4B kickoff (or "Phase 4A ✅ Complete")

 S9 Step 4/4 — Handoff Preparation:
   Overwrite prompt.text with 4B kickoff handoff (minimal pointer format per SI-1)
   4B = Knowledge Graph + Relationship Engine (next phase)

 END OF S9 HANDOFF. REFER TO STATE.md + STANDING_INSTRUCTIONS.md FOR FULL CONTEXT.
════════════════════════════════════════════════════════════════════
