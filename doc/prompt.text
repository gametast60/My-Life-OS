════════════════════════════════════════════════════════════════════
 PHASE 4B — S10: Knowledge Graph + Relationship Engine (Kickoff)
 My Life OS — Brain Intelligence Engine (BIE)
════════════════════════════════════════════════════════════════════

 สิ่งแรกที่ต้องทำ:
 1. อ่าน /doc/STATE.md (current step = S10, constraints, allowed files)
 2. ปฏิบัติตาม /doc/STANDING_INSTRUCTIONS.md (ทุก workflow rule)

 PRIMARY DELIVERABLE (S10 = KG Type + Schema Design):
 - KG Node/Edge TypeScript interface contracts (additive widening of types.ts)
 - Graph storage schema design: bie_graph_nodes + bie_graph_edges table shapes
 - Entity resolution + duplicate detection stubs (no runtime logic yet)
 - Edge proposal queue structure (applied=false HITL invariant, P4-12)
 - Handoff S10 → S11 via STATE.md + prompt.text

 WORK ORDER — EXECUTE IN ORDER:

 S10 Step 1 of 5 — Read-only exploration:
   Read src/pie/bie/types.ts → understand all existing BIE interfaces/types
   Read /doc/ROADMAP_ARCHIVE.md → find Phase 4B Scope + Deliverables section
   Read src/pie/bie/RoomBrainIntelligenceRepository.ts → understand existing 28-method shape

 S10 Step 2 of 5 — KG Type Contracts (additive, types.ts widening):
   Define: GraphNodeType enum (Entity, Tag, Dimension, Evidence, Room, Insight)
   Define: GraphEdgeType enum (6 edge kinds per ROADMAP 4B spec)
   Define: BIEGraphNode interface + BIEGraphEdge interface
   Define: GraphEdgeProposal interface (applied: false invariant)
   Rule: ADDITIVE ONLY — zero removal of existing exported types

 S10 Step 3 of 5 — Storage Schema (no DB wiring yet, type-level only):
   Define: BIEGraphNodeRow (DB row shape for bie_graph_nodes)
   Define: BIEGraphEdgeRow (DB row shape for bie_graph_edges)
   Document: schema rationale in inline JSDoc (column names, index plan)

 S10 Step 4 of 5 — Repository Method Stubs (RoomBrainIntelligenceRepository.ts additive):
   Add stub signatures only (throw new Error("TODO S10") bodies):
   - saveGraphNode / getGraphNode / deleteGraphNode
   - saveGraphEdge / getGraphEdgesByNode / deleteGraphEdge
   - proposeEdge (returns GraphEdgeProposal with applied=false)
   - findDuplicateCandidates (entity resolution stub)
   Rule: Additive widening only — existing 28 methods UNTOUCHED

 S10 Step 5 of 5 — Doc Closeout + Handoff:
   npm run build = Exit 0 (verify no TS errors from new types)
   npm run lint  = Exit 0
   CHANGELOG.md: append S10 section at TOP (≤15 lines per SI-2)
   ROADMAP.md: S10 row → ✅ Complete (targeted cell edit per SI-4)
   STATE.md: update Current Step → S11 kickoff
   prompt.text: overwrite with S11 handoff (minimal pointer format per SI-1)

 END OF S10 HANDOFF PROMPT.
════════════════════════════════════════════════════════════════════
